import 'package:flutter/material.dart';

abstract final class AppElevation {
  static const double surfaceShadowOpacity = 0.035;
  static const double surfaceShadowBlur = 16;
  static const Offset surfaceShadowOffset = Offset(0, 6);
}
